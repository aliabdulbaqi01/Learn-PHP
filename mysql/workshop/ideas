Routes:
// create a route file that will have
        - associative array that have all routes
        - path => view
by add function you can add this function to the array
by route function you can get the get the route




// project description (to do list)
/*
 * user has many tasks
 * tasks belongs to one user
 * User relation:
 *      - id
 *      - username
 *      - email
 *      - password
 *      - img
 *      - task_id


 * Task relations
 *      - id
 *      - title
 *      - desc
 *      - img
 *      - status
 */
